rules:
- id: new-rule
  message: "autofix"
  languages: [generic]
  severity: WARNING
  metadata:
    category: security
  patterns:
  - pattern: |
      return {$...RETARGS};
  - pattern-inside: |
      $RET $NAME(...) {
        ...
      }
  fix: |
      return ($RET){$...RETARGS};
- id: awawawa
  message: "autofix"
  languages: [generic]
  severity: WARNING
  metadata:
    category: security
  patterns:
  - pattern: |
      oklab_to_linear_srgb({$...RETARGS})
  - pattern-inside: |
      $RET $NAME(...) {
        ...
      }
  fix: |
      oklab_to_linear_srgb((Lab){$...RETARGS})